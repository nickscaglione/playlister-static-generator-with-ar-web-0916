<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>

  <body>
    <h1>Songs - <%= Song.count %></h1>
      <ul>
        <% Song.all.each do |song| %>
        <li><h4><a href="/songs/<%=song.to_slug%>.html"><%= song.artist.name %> - <%= song.name %> - <%= song.genre.name %></a></h4></li>
        <% end %>
      </ul>
  </body>
</html>
